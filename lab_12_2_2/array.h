#ifndef __ARRAY_H
#define __ARRAY_H

#include <stdio.h>
#include <stdlib.h>

void fibb_arr(int *array, const int size);

void cp_no_dublicates(int *new_array, const int *const cp_array, int *new_size, const int cp_size);

void printf_array(const int *const array, const int size);

#endif
