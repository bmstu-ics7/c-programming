#include <math.h>

#ifndef DEG_MAT_H
#define DEG_MAT_H

#define N 100

void deg_mat(const int* const a, const int size_a,
            const int* const b, const int size_b,
            int* c, int* size_c);

#endif
